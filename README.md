# meta-dualsense

Yocto layer that builds a headless BeagleBone Black image which presents
to a host PC as a Sony DualSense controller, driven by a USB keyboard and
mouse plugged into the BBB.

See `kas.yml` for the build configuration. Full documentation lands in a
later commit.
